// Solution for LC 344. Reverse String problem
class Solution:
    def reverseString(self, s: List[str]) -> None:
        x=len(s)-1
        for i in range(int(len(s)/2)):
            s[i],s[x-i]=s[x-i],s[i]
        """
        Do not return anything, modify s in-place instead.
        """
        
