class Solution {
public:
    int getDecimalValue(ListNode* head) {
        int cnt=0;
        int ans=0;
        while(head){
            ans =ans * 2 + head->val;
            head= head->next;
            cnt++;
        }
        return ans;
    }
};
